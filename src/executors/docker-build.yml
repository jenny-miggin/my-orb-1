description: >
  This executor can be used for simple jobs
docker:
  - image: 'cimg/base:<<parameters.tag>>'
parameters:
  tag:
    default: "2021.11"
    description: >
      Pick a specific cimg/base image variant:
      https://hub.docker.com/r/cimg/base/tags
    type: string
